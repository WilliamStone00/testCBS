@model CBS.API.Helper.AuthRequest
@{
    Layout = "~/Views/Shared/_LoginMainCover.cshtml";
    ViewBag.Title = "T R U S T B A N K-Authentication";

}


<div class="authentication-inner row m-0">
    <!-- /Left Section -->
    <div class="d-none d-lg-flex col-lg-7 col-xl-8 align-items-center justify-content-center p-5 pb-2">
        <img src="~/AppFiles/Images/TrusBank_LoginBG.png" class="authentication-image" alt="mask" />
    </div>

    <!-- /Left Section -->
    <!-- Login -->
    <div class="d-flex col-12 col-lg-5 col-xl-4 align-items-center authentication-bg position-relative py-sm-5 px-4 py-4">
        <div class="w-px-400 mx-auto pt-5 pt-lg-0">
            <div class="image-container">
                <img src="~/AppFiles/Images/Logog.png" />
            </div>
            <hr />
            <h4 class="mb-2 trustbank-text">T R U S T B A N K <span class="authentication-text">Authentication</span></h4>
            <hr />
            <p class="mb-4">Please sign-in to your account and start the adventure</p>

            @using (Html.BeginForm("Login", "Authentication", new { ReturnUrl = ViewBag.ReturnUrl }, FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal form-label-left" }))
            {
                <div class="form-floating form-floating-outline mb-3">
                    @Html.TextBoxFor(m => m.UserName, new { @class = "form-control", @placeholder = "User name" })
                    @Html.ValidationMessageFor(m => m.UserName, "", new { @class = "text-danger" })

                    <label for="email">Email or Username</label>
                </div>
                <div class="mb-3">
                    <div class="form-password-toggle">
                        <div class="input-group input-group-merge">
                            <div class="form-floating form-floating-outline">
                                @Html.PasswordFor(m => m.Password, new { @class = "form-control", @placeholder = "Password" })
                                @Html.ValidationMessageFor(m => m.Password, "", new { @class = "text-danger" })
                                <label for="password">Password</label>
                            </div>
                            <span class="input-group-text cursor-pointer"><i class="mdi mdi-eye-off-outline"></i></span>
                        </div>
                    </div>
                </div>
                <div class="mb-3 d-flex justify-content-between">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="remember-me" />
                        <label class="form-check-label" for="remember-me"> Remember Me </label>
                    </div>
                    <a href="/" class="float-end mb-1">
                        <span>Forgot Password?</span>
                    </a>
                </div>
                <button class="btn btn-primary d-grid w-100">Login</button>
            }

            @*<p class="text-center mt-2">
            <span>New on our platform?</span>
            <a href="auth-register-cover.html">
                <span>Create an account</span>
            </a>
        </p>*@
            <br />
            <div class="mb-3 d-flex justify-content-between">
                <div class="col-sm-12">
                    <p class="text-muted mb-0"><b>LOGIN POLICY</b></p>
                    <hr />
                    <label>1. Your max login attempt is 3, else blocked.</label>
                    <label>2. If your account is blocked, contact your administrator.</label>
                    <label>3. If you forget your password, contact your administrator.</label>
                </div>
            </div>

            @if (ViewBag.Success == false)
            {
                <div class="alert alert-outline-danger" role="alert">
                    @ViewBag.Message;
                </div>
            }
        </div>
    </div>
    <!-- /Login -->
</div>



@section Scripts {@Scripts.Render("~/bundles/jqueryval")}