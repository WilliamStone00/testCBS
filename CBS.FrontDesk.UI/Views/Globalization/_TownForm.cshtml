@model CBS.FrontDesk.Data.Entity.Config.GlobalizationViewModel
@using Resources
<!-- Section Title -->
<div class="col-12">
    <h6 class="text-primary border-bottom pt-2 pb-2">
        <i class="bi bi-geo-alt"></i> @GlobalGlobalization.Town_Management
    </h6>
</div>

<!-- Town Form -->
@using (Html.BeginForm("SaveTown", "Globalization", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "return AjaxPostAndUpdate(this);" }))
{
    <div class="row g-3 mb-4" id="townForm">
        <div class="col-md-6">
            <div class="form-floating shadow-sm">
                <select id="SubDivisionId" name="Town.SubdivisionId" class="form-select select2" required>
                    <option value="">--- @GlobalShared.Select_Sub_Division ---</option>
                    @foreach (var sub in Model.SubDivisions)
                    {
                        <option value="@sub.Id">@sub.Name</option>
                    }
                </select>
                <label for="SubDivisionId">@GlobalShared.SubDivision</label>
            </div>
        </div>
        <div class="col-md-6">
            <div class="form-floating shadow-sm">
                <input type="text" name="Town.Name" class="form-control" placeholder="@GlobalGlobalization.Town_Name" required />
                <label>@GlobalGlobalization.Town_Name</label>
            </div>
        </div>
        <div class="col-12 text-end">
            <button type="submit" class="btn btn-warning px-4">
                <i class="bi bi-save2"></i> @GlobalGlobalization.Save_Town
            </button>
        </div>
    </div>
}

<!-- Success Message -->
<div id="successMessageTown" class="alert alert-success d-none"></div>
