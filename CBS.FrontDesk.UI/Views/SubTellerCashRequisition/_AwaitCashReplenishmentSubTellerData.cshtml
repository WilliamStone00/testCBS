@model List<CBS.FrontDesk.Data.Entity.SavingProducts.CashReplenishmentSubTeller>
@using Resources
<fieldset class="fieldset-custom">
    <legend id="select_base">@GlobalShared.DataListings </legend>
    @if (Model.Count() == 0)
    {
        <div class="text-center my-3">
            <p>@GlobalShared.NoDataFound</p>
        </div>
    }
    else
    {
        <div class="table-responsive mb-3">
            <table class="table table-hover" id="myDataTable">
                <thead class="table-light">
                    <tr>
                        <th>@GlobalShared.Date</th>
                        <th>@GlobalSubTellerCashRequisition.Amount</th>
                        <th>@GlobalSubTellerCashRequisition.Branch</th>
                        <th>@GlobalShared.Status</th>
                        <th>@GlobalSubTellerCashRequisition.InitiatedBy</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var d in Model)
                    {
                        <tr>
                            <td style="width:20%">@d.InitializeDate</td>
                            <td style="width:10%">@d.RequestedAmount.ToString("#,##0.0")</td>
                            <td style="width:30%">@d.Branch.Name</td>
                            <td style="width:10%">
                                @if (d.ApprovedStatus == "Approved")
                                {
                                    <span class="badge bg-info">@GlobalShared.Approved</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">@d.ApprovedStatus</span>
                                }
                            </td>
                            <td style="width:20%">@d.RequesterName</td>
                            <td style="width:10%">
                                <a href="/SubTellerCashRequisition/RequestValidation?ProvisionKey=@d.Id" class="mr-2" data-toggle="tooltip" data-placement="top" title="@GlobalShared.Validate @d.RequestedAmount @GlobalSubTellerCashRequisition.intoOperation.">@GlobalShared.Validate</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>



    }


</fieldset>
