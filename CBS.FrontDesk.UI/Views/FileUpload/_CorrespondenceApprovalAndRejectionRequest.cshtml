@model CBS.FrontDesk.Data.Entity.Accounting_V2.FileUpload.CorrespondenceApproveAndRejection
@using Resources
@using (Html.BeginForm("ApproveOrRejectCorrespondenceRequest", "FileUpload", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "return AjaxPostAndUpdate(this);" }))
{

    @Html.AntiForgeryToken()
    <div class="card mb-3 shadow-sm">
        <div class="card-header d-flex justify-content-between align-items-center text-primary">
            <span>
                <i class="mdi mdi-checkbox-marked-circle-outline me-2"></i>
                @GlobalFileUpload.Correspondence_Request_Approval_Rejection
            </span>
            @if (Model.Type == "Approval")
            {
                <span class="badge bg-info text-dark">
                    @GlobalShared.Approval_Request
                </span>
            }
            else if (Model.Type == "Rejection")
            {
                <span class="badge bg-info text-dark">
                    @GlobalShared.Rejection_Request
                </span>
            }

        </div>

        <div class="card-body">
            <!-- Hidden Id / Language (for post) -->
            @Html.HiddenFor(m => m.Id, new { id = "CorrespondenceId" })
            @Html.HiddenFor(m => m.Type)
            @Html.HiddenFor(m => m.Language, new { id = "CorrespondenceLanguage" })

            <!-- Tips block -->
            <div class="alert alert-light border mb-3">
                <div class="d-flex">
                    <div class="me-2">
                        <i class="mdi mdi-information-outline text-primary fs-4"></i>
                    </div>
                    <div>
                        <h6 class="fw-bold text-primary mb-1">@GlobalShared.Approval_tips</h6>
                        <ul class="mb-0 ps-3">
                            <li>@GlobalFileUpload.Review_the_correspondence_details_before_approving_or_rejecting</li>
                            <li>@GlobalFileUpload.Use_the_note_field_to_explain_the_reason_for_your_decision</li>
                            <li>@GlobalFileUpload.Your_user_identity_will_be_attached_automatically_for_audit_trail</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Basic info (read-only) -->
            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label fw-bold">
                        <i class="mdi mdi-identifier me-1"></i> Id
                    </label>
                    <input type="text"
                           class="form-control"
                           value="@Model.Id"
                           readonly />
                </div>

                <div class="col-md-6">
                    <label class="form-label fw-bold">
                        <i class="mdi mdi-account-check-outline me-1"></i> @GlobalShared.Requested_by
                    </label>
                    <input type="text"
                           class="form-control"
                           value="@Model.RequestBy"
                           readonly />
                </div>
            </div>

            <!-- Note field -->
            <div class="mb-3">
                @Html.LabelFor(m => m.Note, new { @class = "form-label fw-bold text-primary" })
                @Html.TextAreaFor(
                    m => m.Note,
                    new
                    {
                        @class = "form-control",
                        rows = "4",
                        placeholder = "Enter approval note (required for audit)...",
                        id = "CorrespondenceNote"
                    })
                <small class="text-muted">
                    @GlobalFileUpload.Provide_a_clear_justification_This_note_will_be_stored_in_the_audit_trail
                </small>
            </div>

            <!-- Optional: language display (read-only) -->
            @* @if (!string.IsNullOrWhiteSpace(Model.Language))
                {
                    <div class="mb-2">
                        <label class="form-label fw-bold">
                            <i class="mdi mdi-translate me-1"></i> Language
                        </label>
                        <input type="text"
                               class="form-control"
                               value="@Model.Language"
                               readonly />
                    </div>
                }*@
        </div>

        <!-- Footer with Approve button -->
        <div class="card-footer d-flex justify-content-end">
            @if (Model.Type == "Approval")
            {
                <button type="submit" class="btn btn-success" id="btnApproveCorrespondence" data-bs-dismiss="modal">
                    <i class="mdi mdi-check-circle-outline me-1"></i> @GlobalShared.Approve
                </button>
            }
            else
            {
                <button type="submit" class="btn btn-danger" id="btnRejectCorrespondence"  data-bs-dismiss="modal">
                    <i class="mdi mdi-check-circle-outline me-1"></i> @GlobalShared.Reject
                </button>
            }

        </div>
    </div>
}
