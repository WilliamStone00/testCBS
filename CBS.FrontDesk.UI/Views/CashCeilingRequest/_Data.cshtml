@model List<CBS.FrontDesk.Data.Entity.CashCeilingManagement.CashCeilingRequest>
    @using Resources
<fieldset class="fieldset-custom">
    <legend id="select_base">@CashCeilingRequest_InitiatorData.CashCeilingRequests</legend>
    @if (!Model.Any())
    {
        <div class="text-center my-3">
            <p class="text-muted">@CashCeilingRequest_InitiatorData.NoCashCeilingRequestsHaveBeenMadeYet.</p>
        </div>
    }
    else
    {
        <div class="table-responsive mb-3">
            <table class="table table-hover table-bordered text-center" id="myDataTable">
                <thead class="table-light">
                    <tr>
                        <th style="width: 20%;">@CashCeilingRequest_InitiatorData.Branch</th>
                        <th style="width: 15%;">Requested By</th>
                        <th style="width: 15%;">@CashCeilingRequest_InitiatorData.RequestType</th>
                        <th style="width: 15%;">@CashCeilingRequest_InitiatorData.Amount</th>
                        <th style="width: 15%;">@CashCeilingRequest_InitiatorData.Status</th>
                        <th style="width: 20%;">@CashCeilingRequest_InitiatorData.Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var request in Model)
                    {
                        <tr>
                            <td>@request.BranchName</td>
                            <td>@request.RequestedBy</td>
                            <td>
                                @if (request.RequestType == "Cash_To_Vault")
                                {
                                    <span class="badge bg-primary">@CashCeilingRequest_InitiatorData.CashToVaultt</span>
                                }
                                else if (request.RequestType == "Subteller_Cash_To_PrimaryTeller")
                                {
                            <span class="badge bg-warning">@CashCeilingRequest_InitiatorData.SubTellerToPrimaryTeller</span>
                                }
                                else
                                {
                            <span class="badge bg-secondary">@CashCeilingRequest_InitiatorData.Unknown</span>
                                }
                            </td>
                            <td>@request.CashoutRequestAmount.ToString("#,##0.0") FCFA</td>
                            <td>
                                @if (request.ApprovedStatus == "Pending")
                                {
                                    <span class="badge bg-warning">@CashCeilingRequest_RequestValidation.Pending</span>
                                }
                                else if (request.ApprovedStatus == "Approved")
                                {
                            <span class="badge bg-success">@CashCeilingRequest_RequestValidation.Approved</span>
                                }
                                else if (request.ApprovedStatus == "Rejected")
                                {
                            <span class="badge bg-danger">@CashCeilingRequest_RequestValidation.Rejected</span>
                                }
                                else
                                {
                            <span class="badge bg-secondary">@CashCeilingRequest_InitiatorData.Unknown</span>
                                }
                            </td>
                            <td>
                                <div class="d-flex justify-content-center align-items-center gap-2">
                                    <!-- Validate Button -->
                                    <button type="button"
                                            class="btn btn-sm btn-outline-primary"
                                            onclick="validateRequest('@request.Id')"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="top"
                                            title="Validate Request">
                                        <i class="fas fa-check"></i> @GlobalShared.Validate
                                    </button>

                                    <!-- Details Button -->
                                    <button type="button"
                                            class="btn btn-sm btn-outline-info"
                                            onclick="showRequestDetails('@request.Id')"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="top"
                                            title="View Details">
                                        <i class="fas fa-info-circle"></i> @GlobalShared.Details
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</fieldset>

<script>
    function validateRequest(requestId) {
        $('#vaultDetailsModal').modal('show');
        $('#vaultDetailsContent').html('<p>Loading validation form...</p>');

        $.ajax({
            url: '/CashCeiling/ValidateRequest',
            type: 'GET',
            data: { id: requestId },
            success: function (data) {
                $('#vaultDetailsContent').html(data);
            },
            error: function () {
                $('#vaultDetailsContent').html('<p class="text-danger">Error loading validation form.</p>');
            }
        });
    }

    function showRequestDetails(requestId) {
        $('#vaultDetailsModal').modal('show');
        $('#vaultDetailsContent').html('<p>Loading request details...</p>');

        $.ajax({
            url: '/CashCeiling/GetRequestDetails',
            type: 'GET',
            data: { id: requestId },
            success: function (data) {
                $('#vaultDetailsContent').html(data);
            },
            error: function () {
                $('#vaultDetailsContent').html('<p class="text-danger">Error loading request details.</p>');
            }
        });
    }
</script>
