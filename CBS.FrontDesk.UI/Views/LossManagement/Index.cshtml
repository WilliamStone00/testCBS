@model CBS.FrontDesk.Data.Entity.CheckManagementSystem.LossManagementSystem.CustomerCheckbooksDto
@using Resources
@{
    ViewBag.Title = "Loss Management";
}

<div class="row">
    <div class="col-12">
        <div class="card bg-light mb-2">
            <div class="card-body text-center">
                <h1 class="card-title text-primary">
                    <i class="mdi mdi-book-alert me-2"></i> @GlobalLossManagement.loss_management_system
                </h1>
                <p class="card-text">
                    @GlobalLossManagement.search_for_customer_checkbooks_and_manage_loss_declarations_efficiently.
                </p>
                <p class="card-text fw-bold">
                    <i class="mdi mdi-alert-circle-outline me-1"></i> @GlobalLossManagement.use_the_filters_below_to_narrow_down_checkbook_search_results.
                </p>
                <hr style="border: 2px solid rgba(128, 128, 128, 0.5);" />
            </div>
        </div>
    </div>
</div>

<div class="card mb-3">
    <h6 class="card-header d-flex justify-content-between align-items-center text-primary">
        <span><i class="mdi mdi-information-outline me-2"></i> @GlobalLossManagement.loss_management_guidelines</span>
        <button class="btn btn-light btn-sm" type="button" data-bs-toggle="collapse" data-bs-target="#collapseGuidelines" aria-expanded="false">
            <i class="mdi mdi-chevron-down"></i>
        </button>
    </h6>
    <div id="collapseGuidelines" class="collapse show">
        <div class="card-body">
            <ul>
                <li><i class="mdi mdi-account-check-outline text-success me-2"></i><strong>@GlobalLossManagement.customer_member_reference:</strong> @GlobalLossManagement.unique_identifier_for_the_customer_whose_checkbooks_you_want_to_manage.</li>
                <li><i class="mdi mdi-book-search text-info me-2"></i><strong>@GlobalLossManagement.checkbook_search:</strong> @GlobalLossManagement.enter_the_customer_reference_to_retrieve_all_associated_checkbooks.</li>
                <li><i class="mdi mdi-eye-outline text-primary me-2"></i><strong>@GlobalLossManagement.view_check_leaves:</strong> @GlobalLossManagement.inspect_individual_checks_within_each_checkbook_for_detailed_status.</li>
                <li><i class="mdi mdi-alert-outline text-warning me-2"></i><strong>@GlobalLossManagement.loss_reporting:</strong> @GlobalLossManagement.report_lost_or_stolen_checkbooks_or_individual_check_leaves_promptly.</li>
                <li><i class="mdi mdi-book-information-variant text-secondary me-2"></i><strong>@GlobalShared.Detailed_Information:</strong> @GlobalLossManagement.access_comprehensive_details_for_each_checkbook_and_its_leaves.</li>
            </ul>
        </div>
    </div>
</div>

<!-- Advanced Filters -->
<div class="card shadow p-3 mb-4">
    <h5 class="text-primary mb-3"><i class="mdi mdi-filter-outline me-1"></i> @GlobalLossManagement.checkbook_search_filters</h5>
    <form id="checkbookFilterForm">
        <div class="card p-3 mb-3 bg-light">
            <!-- Toggle switches for additional filters -->
            <div class="row mb-3">
                <div class="col-md-4">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="toggleStatus" onclick="document.getElementById('statusFilter').classList.toggle('d-none')">
                        <label class="form-check-label fw-bold" for="toggleStatus">@GlobalShared.Show_Status_Filter</label>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="toggleCategory" onclick="document.getElementBy                             *
                            Id('categoryFilter').classList.toggle('d-none')">
                        <label class="form-check-label fw-bold" for="toggleCategory">@GlobalShared.show_category_filter</label>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="form-check">
                        <input class="form-check-input" type="checkbox" id="toggleDate" onclick="document.getElementById('dateFilter').classList.toggle('d-none')">
                        <label class="form-check-label fw-bold" for="toggleDate">@GlobalShared.show_date_filter</label>
                    </div>
                </div>
            </div>

            <!-- Default Filters -->
            <div class="row mb-3">
                <div class="col-md-6">
                    <label class="form-label fw-bold">@GlobalLossManagement.customer_member_reference</label>
                    <input type="text" id="customerId" name="customerId" class="form-control" placeholder="@GlobalLossManagement.enter_customer_id" />
                </div>
                <div class="col-md-6">
                    <label class="form-label fw-bold">@GlobalShared.CheckBook_ID</label>
                    <input type="text" id="checkbookId" name="checkbookId" class="form-control" placeholder="@GlobalLossManagement.enter_specific_checkbook_id" />
                </div>
            </div>

            <!-- Status Filter -->
            <div id="statusFilter" class="d-none">
                <div class="row mb-3">
                    <div class="col-md-12">
                        <label class="form-label fw-bold">@GlobalLossManagement.checkbook_status</label>
                        <select id="status" name="status" class="form-select">
                            <option value="">@GlobalShared.All_status</option>
                            <option value="Active">@GlobalShared.Active</option>
                            <option value="Inactive">@GlobalShared.InActive</option>
                            <option value="Blocked">@GlobalShared.Blocked</option>
                            <option value="Lost">@GlobalShared.Lost</option>
                            <option value="Suspended">@GlobalShared.suspended</option>
                        </select>
                    </div>
                </div>
            </div>

            <!-- Category Filter -->
            <div id="categoryFilter" class="d-none">
                <div class="row mb-3">
                    <div class="col-md-12">
                        <label class="form-label fw-bold">@GlobalLossManagement.checkbook_category</label>
                        <select id="category" name="category" class="form-select">
                            <option value="">@GlobalShared.All_Categories</option>
                            <option value="Personal">@GlobalShared.Personal</option>
                            <option value="Business">@GlobalShared.business</option>
                            <option value="Savings">@GlobalShared.Savings</option>
                            <option value="Premium">@GlobalShared.premium</option>
                            <option value="Corporate">@GlobalShared.Corporate</option>
                        </select>
                    </div>
                </div>
            </div>

            <!-- Date Filter -->
            <div id="dateFilter" class="d-none">
                <div class="row mb-3">
                    <div class="col-md-6">
                        <label class="form-label fw-bold">@GlobalShared.issue_date_from</label>
                        <input type="date" id="issueDateFrom" name="issueDateFrom" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label class="form-label fw-bold">@GlobalShared.issue_date_to</label>
                        <input type="date" id="issueDateTo" name="issueDateTo" class="form-control" />
                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-2">
            <div class="col-md-12 d-flex">
                <button id="applyFilterBtn" class="btn btn-primary me-2 w-100"><i class="mdi mdi-magnify"></i> @GlobalShared.search_checkbooks</button>
                <button id="resetFilterBtn" type="reset" class="btn btn-secondary me-2 w-100"><i class="mdi mdi-refresh"></i> @GlobalShared.Reset_Filters</button>
                @* <button id="clearResultsBtn" class="btn btn-label-secondary w-100"><i class="mdi mdi-close"></i> Clear Results</button>*@
            </div>
        </div>
    </form>
</div>



<div id="userTableView">
    @Html.Partial("_CheckbooksTable")
</div>
@*<div id="datalistingview">
        @Html.Partial("_CheckbooksTable")
    </div>*@
<!-- DataTable Partial -->
<!--<div id="datalistingview">-->
<!-- Checkbooks table will be loaded here via DataTable -->
<!--</div>-->
<!-- Modal Structure -->
<div class="modal fade" id="mainModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="modalTitle">@GlobalShared.CheckBook_Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body" id="modalBody">
                <!-- Content will be loaded here via AJAX -->
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-label-secondary" data-bs-dismiss="modal">@GlobalShared.Close</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
<script>
        /* ================================
           🔧 1. Generic Utility Functions
        ================================= */

        // Universal DataTable initializer
        function initDataTable(tableId, url, columns, getFiltersFn) {
            return $(tableId).DataTable({
                serverSide: true,
                destroy: true,
                searching: false,
                order: [[0, 'asc']],
                ajax: {
                    url: url,
                    type: 'POST',
                    contentType: 'application/json',
                    data: function (d) {
                        const filters = getFiltersFn();
                        filters.options = {
                            draw: d.draw,
                            start: d.start,
                            length: d.length,
                            skip: d.start,
                            pageSize: d.length,
                            sortColumnName: d.columns[d.order[0].column].data,
                            sortColumnDirection: d.order[0].dir
                        };
                        return JSON.stringify(filters);
                    }
                },
                columns: columns,
                language: {
                    emptyTable: "No matching records found",
                    zeroRecords: "No matching records found"
                }
            });
        }

        // Generic modal loader
        function loadModal(title, url, params = {}) {
            $.ajax({
                type: "GET",
                url: url,
                data: params,
                success: function (data) {
                    $("#modalBody").html(data);
                    $("#mainModal .modal-title").html(title);
                    $("#mainModal").modal("show");
                },
                error: function (err) {
                    if (err.status === 401)
                        window.location.href = '/Authentication/Login';
                    else
                        appalert("Error loading content: " + err.statusText, 0, 1);
                }
            });
        }

        // Confirmation wrapper
        function confirmAction(title, message, onConfirm) {
            alertify.confirm(title, message, onConfirm, () => appalert('Operation cancelled', 3, 1))
                .set('labels', { ok: 'Yes, Proceed', cancel: 'Cancel' });
        }

        /* ================================
           📘 2. DataTable Loaders
        ================================= */

    function collectCheckbookFilterParams() {
        return {
            customerId: $('#customerId').val(),
            branchId: $('#branchId').val(),
            checkBookCategoryId: $('#category').val(),
            status: $('#status').val(),
            startDate: $('#issueDateFrom').val(),
            endDate: $('#issueDateTo').val(),
            options: {}
        };
    }

        function loadCheckbooksData() {
            initDataTable('#myDataTable', '@Url.Action("LoadCheckbooksData", "LossManagement")', [
                { data: 'CreatedDate', render: d => d ? moment(d).format('DD/MM/YYYY HH:mm:ss') : '' },
                { data: 'CategoryName' },
                { data: 'CustomerId' },
                { data: 'NumberOfLeaves' },
                { data: 'Status' },
                {
                    data: 'Id', orderable: false,
                    render: id => `
                        <div class="btn-group btn-group-sm">
                            <a href="@Url.Action("CheckbookDetails", "LossManagement")?KEY=${id}"
                               class="btn btn-outline-primary">
                               <i class="mdi mdi-book-information-variant me-1"></i> Details
                            </a>
                           <a href="@Url.Action("RequestLossForCheckBook", "LossManagement")?KEY=${id}"
                                   class="btn btn-sm btn-outline-warning">
                                   <i class="mdi mdi-alert-outline me-1"></i> Req. Lost
                                </a>
                        </div>`
                }
            ], collectCheckbookFilterParams);
        }

      
        /* ================================
           🧾 3. Loss Request Handlers
        ================================= */

        function submitLossRequest(form) {
            $.validator.unobtrusive.parse(form);
            if (!$(form).valid()) return false;

            confirmAction("Confirm Loss Request", `
                <div class="text-start">
                    <p>📝 This loss request will be <strong>submitted</strong> for processing.</p>
                    <p><strong>Do you want to proceed?</strong></p>
                </div>`, () => {
                    const modalEl = document.getElementById('mainModal');
                    const modal = bootstrap.Modal.getInstance(modalEl);
                    if (modal) modal.hide();

                    $.ajax({
                        type: 'POST',
                        url: form.action,
                        data: new FormData(form),
                        processData: false,
                        contentType: false,
                        success: function (response) {
                            if (response.success) {
                                alertify.success(response.message || 'Loss request submitted successfully');
                                $('#myDataTable').DataTable().ajax.reload();
                            } else {
                                appalert(response.message || 'Error submitting loss request', 2, 1);
                            }
                        },
                        error: function (err) {
                            if (err.status === 401)
                                window.location.href = '/Authentication/Login';
                            else
                                appalert(err.responseJSON?.message || err.statusText || 'An error occurred', 3, 1);
                        }
                    });
                });
            return false;
        }

        function approveLossRequest(requestId) {
            confirmAction('Approve Loss Request', 'Are you sure you want to approve this loss request?', () => {
                $.post('/LossManagement/ApproveLossRequest', { requestId }, response => {
                    response.success ? toastr.success(response.message) : toastr.error(response.message);
                    $('#lossRequestsTable').DataTable().ajax.reload();
                });
            });
        }

        function rejectLossRequest(requestId) {
            alertify.confirm('Reject Loss Request', `
                <div class="text-start">
                    <p>Please enter a reason for rejecting this loss request:</p>
                    <textarea id="rejectReasonTextarea" class="form-control" rows="4" placeholder="Enter rejection reason..."></textarea>
                </div>`, () => {
                    const reason = $('#rejectReasonTextarea').val().trim();
                    if (reason.length >= 10) {
                        $.post('/LossManagement/RejectLossRequest', { requestId, reason }, response => {
                            response.success ? appalert(response.message, 1, 1) : toastr.error(response.message);
                            $('#lossRequestsTable').DataTable().ajax.reload();
                        });
                    } else {
                        appalert("Rejection reason must be at least 10 characters.", 2, 1);
                    }
                }, () => toastr.info("Rejection cancelled."));
        }

        /* ================================
           🚀 4. Initialization
        ================================= */

        $(document).ready(function () {
            $('#applyFilterBtn').on('click', e => {
                e.preventDefault();
                if ($.fn.DataTable.isDataTable('#myDataTable')) $('#myDataTable').DataTable().destroy();
                loadCheckbooksData();
            });

            $('#resetFilterBtn').on('click', e => {
                e.preventDefault();
                $('#checkbookFilterForm').trigger('reset');
                if ($.fn.DataTable.isDataTable('#myDataTable')) $('#myDataTable').DataTable().destroy();
            });

            $('#clearResultsBtn').on('click', e => {
                e.preventDefault();
                if ($.fn.DataTable.isDataTable('#myDataTable')) $('#myDataTable').DataTable().destroy();
            });

            $('#issueDateFrom').val(new Date().toISOString().split('T')[0]);
        });
</script>
}
