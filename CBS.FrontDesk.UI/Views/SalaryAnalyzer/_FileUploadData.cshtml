@model CBS.FrontDesk.Data.Entity.SalaryManagement.SalaryUploadModelCarrier
@using Resources


<style>
    .status-pip {
        display: inline-block;
        width: 10px;
        height: 10px;
        border-radius: 50%;
        vertical-align: middle
    }

        .status-pip.online {
            background: #28a745;
            box-shadow: 0 0 0 2px rgba(40,167,69,.15)
        }

        .status-pip.offline {
            background: #6c757d;
            box-shadow: 0 0 0 2px rgba(108,117,125,.15)
        }

    .truncate {
        max-width: 320px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis
    }

    .truncate-branch {
        max-width: 120px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis
    }

    .truncate-user {
        max-width: 120px;
        white-space: nowrap;
        overflow: hidden;
        text-overflow: ellipsis
    }

    #myDataTable thead th {
        white-space: nowrap
    }

    /* Optional: squeeze a bit more on smaller screens */
    @@media (max-width: 1400px) {
        .truncate-branch {
            max-width: 100px
        }

        .truncate-user {
            max-width: 100px
        }
    }
</style>

<fieldset class="fieldset-custom">
    <div class="mb-3 border-2">
        <div id="datalistingview_inner">
            <div class="table-responsive mb-3">
                @if (Model.FileUploads != null && Model.FileUploads.Any())
                {
                    <table class="table table-hover align-middle" id="myDataTable">
                        <thead class="table-light">
                            <tr>
                                <th>@GlobalShared.File_Name</th>
                                <th>@GlobalShared.File_Type</th>
                                <th>@GlobalShared.Branch</th>
                                <th>@GlobalShared.visibility</th>
                                <th>@GlobalShared.Uploaded_By</th>
                                <th>@GlobalShared.Uploaded_On</th>
                                <th style="width:1%;">@GlobalShared.Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var file in Model.FileUploads)
                            {
                                <tr>
                                    <!-- File name (truncated) -->
                                    <td class="truncate" title="@file.FileName">@file.FileName</td>

                                    <!-- File type -->
                                    <td>
                                        @if (!string.IsNullOrWhiteSpace(file.FileType))
                                        {<span class="badge bg-secondary">@file.FileType</span> }
                                    else
                                    { <span class="badge bg-light text-muted">@GlobalShared.Unknown</span>}
                                    </td>

                                    <!-- Branch -->
                                    <td class="truncate-branch" title="@file.BranchName">
                                        @if (!string.IsNullOrWhiteSpace(file.BranchName))
                                        {<span class="badge bg-label-primary">@file.BranchName</span> }
                                    else
                                    { <span class="badge bg-light text-muted">N/A</span>}
                                    </td>

                                    <!-- Visibility -->
                                    <td>
                                        @if (file.PrivateView)
                                        {
                                            <span class="status-pip offline" title="@GlobalSalaryAnalyzer.private_visible_to_this_branch_only"></span>
                                            <span class="ms-1">@GlobalShared._private</span>
                                        }
                                        else
                                        {
                                            <span class="status-pip online" title="@GlobalSalaryAnalyzer.public_visible_to_other_branches"></span>
                                            <span class="ms-1">@GlobalShared._public</span>
                                        }
                                    </td>

                                    <td class="truncate-user" title="@file.UploadedBy">@file.UploadedBy</td>
                                    <td>@file.UploadedOn.ToString("dd/MM/yyyy HH:mm")</td>

                                    @*<td>
                                        @if (!string.IsNullOrEmpty(file.SalaryProcessingStatus))
                                        {<span class="badge bg-info">@file.SalaryProcessingStatus</span> }
                                    else
                                    { <span class="badge bg-secondary">N/A</span>}
                                    </td>*@

                                    <!-- Actions: condensed dropdown -->
                                    <td>
                                        <div class="d-flex justify-content-center align-items-center gap-2">

                                            <!-- Details Button -->
                                            <a type="button"
                                               class="btn btn-sm btn-outline-info d-flex align-items-center"
                                               href="/SalaryAnalyzer/Detail?fileUploadid=@file.Id"
                                               data-bs-toggle="tooltip" target="_blank"
                                               data-bs-placement="top"
                                               title="View Details">
                                                <i class="fas fa-info-circle me-1"></i> @GlobalShared.Details
                                            </a>

                                        </div>
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                }
                else
                {
                    <div class="text-center my-3">
                        <p>@GlobalShared.NoFilesUploadedYet.</p>
                    </div>
                }
            </div>
        </div>
    </div>
</fieldset>
