
@model CBS.FrontDesk.Data.Entity.SalaryManagement.SalaryUploadModelCarrier
    @using Resources
@{
    ViewBag.Title = "Salary Summary";
}
<div class="row">
    <div class="col-xl-12 col-lg-12 col-md-12 order-0 order-md-1">
        <div class="card">
            <!-- Page Header -->
            <div class="card-header text-center bg-light">
                <h1 class="card-title text-primary">
                    <i class="mdi mdi-file-cabinet me-2"></i> @GlobalSalaryAnalyzer.salary_summary
                </h1>
                <p class="card-text text-muted">
                    @GlobalSalaryAnalyzer.view_analyze_and_manage_employee_salary.
                </p>
                <hr style="border: 2px solid rgba(128, 128, 128, 0.5);" />
            </div>

            <!-- Tips Section -->
            <div class="card mb-2">
                <h6 class="card-header d-flex justify-content-between align-items-center text-primary">
                    <span><i class="mdi mdi-lightbulb-on-outline me-2"></i> @GlobalShared.Tips</span>
                    <button class="btn btn-light btn-sm" type="button" data-bs-toggle="collapse" data-bs-target="#collapseSalarySummaryTips" aria-expanded="false" aria-controls="collapseSalarySummaryTips">
                        <i class="mdi mdi-chevron-down"></i>
                    </button>
                </h6>
                <div id="collapseSalarySummaryTips" class="collapse">
                    <div class="card-body">
                        <ul class="mb-0">
                            <li><i class="mdi mdi-file-document-outline text-primary me-2"></i> @GlobalShared.use_the <strong>"@GlobalSalaryAnalyzer.run_salary_analysis"</strong> @GlobalSalaryAnalyzer.button_to_analyze_uploaded_salary_data.</li>
                            <li><i class="mdi mdi-eye-outline text-primary me-2"></i> @GlobalShared.click_the <strong>"@GlobalSalaryAnalyzer.view_result_of_analyzed_salary"</strong> @GlobalSalaryAnalyzer.button_to_check_previously_analyzed_results.</li>
                            <li><i class="mdi mdi-alert-outline text-warning me-2"></i> @GlobalSalaryAnalyzer.ensure_all_uploaded_salary_files_are_accurate (e.g., Excel, CSV).</li>
                            <li><i class="mdi mdi-check-circle-outline text-success me-2"></i> @GlobalSalaryAnalyzer.regularly_verify_analyzed_results.</li>
                            <li><i class="mdi mdi-database-search-outline text-primary me-2"></i> @GlobalSalaryAnalyzer.use_search_filters_to_quickly_locate_specific_employee_salary_records.</li>
                            <li><i class="mdi mdi-alert-decagram-outline text-danger me-2"></i> @GlobalSalaryAnalyzer.address_anomalies_like_overpayments.</li>
                            <li><i class="mdi mdi-pencil-outline text-success me-2"></i> @GlobalSalaryAnalyzer.edit_analyzed_salary_records_for_corrections.</li>
                            <li><i class="mdi mdi-delete-outline text-danger me-2"></i> @GlobalSalaryAnalyzer.delete_invalid_uploads_to_maintain_clean.</li>
                        </ul>
                    </div>
                </div>
            </div>

            <!-- Main Section -->
            <div id="accordionPopoutIcon" class="accordion mt-3 accordion-popout">
                <div class="accordion-item active">
                    <h2 class="accordion-header text-body d-flex justify-content-between"
                        id="accordionPopoutIconThree">
                        <button type="button"
                                class="accordion-button"
                                data-bs-toggle="collapse"
                                data-bs-target="#accordionPopoutIcon-3"
                                aria-expanded="true"
                                aria-controls="accordionPopoutIcon-3">
                            <i class="mdi mdi-chart-bar me-1"></i> @GlobalShared.salary_analysis
                        </button>
                    </h2>
                    <div id="accordionPopoutIcon-3"
                         class="accordion-collapse collapse show"
                         data-bs-parent="#accordionPopoutIcon">
                        <div class="accordion-body">
                            <div id="mainviewChange">
                                <div class="change-group">
                                    <div class="card mb-3">
                                        <div class="card-body d-flex justify-content-end align-items-center">
                                            <a type="button" href="/SalaryUpload" class="btn btn-label-primary btn-sm me-2" data-bs-toggle="tooltip" title="@GlobalShared.ViewList">
                                                <i class="mdi mdi-view-list mdi-18px"></i> @GlobalShared.ViewList
                                            </a>
                                            <!-- Run Salary Analysis -->
                                            <a type="button" href="#" class="btn btn-label-secondary btn-sm me-2"
                                               data-bs-toggle="tooltip" title="@GlobalSalaryAnalyzer.run_salary_analysis"
                                               onclick="runAnalysis('@Model.SalaryUploadModelSummaryDto.FileUploadId')">
                                                <i class="mdi mdi-play-box-outline mdi-18px"></i> @GlobalShared.run_analysis
                                            </a>
                                            <!-- View Analyzed Results -->
                                            <a type="button" href="/SalaryAnalyzer/SalaryAnalysisResultSummary?fileUploadid=@Model.SalaryUploadModelSummaryDto.FileUploadId" target="_blank" class="btn btn-label-primary btn-sm" data-bs-toggle="tooltip" title="@GlobalSalaryAnalyzer.view_analyzed_results">
                                                <i class="mdi mdi-file-eye-outline mdi-18px"></i> @GlobalShared.view_results
                                            </a>

                                        </div>
                                    </div>
                                    <div id="datalistingview">
                                        @Html.Partial("_SalaryDetailsData")
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
<script>
        $(document).ready(function () {

            LoadDT("myDataTable", 0)


        });
        function runAnalysis(fileUploadId) {
        alertify.confirm(
          "RUN SALARY ANALYSIS WARNING!!!",
          "Are you sure you want to run the salary analysis?\nThis action cannot be reverted!",
          function () {
            const url = "/SalaryAnalyzer/ExecuteAnalysis";
            const payload = { FileUploadId: fileUploadId };

            $.ajax({
              type: "POST",
              url: url,
              contentType: "application/json",
              data: JSON.stringify(payload),
              success: function (response) {
                if (response && response.success) {
                  // Build the redirect URL safely using Razor to respect app base path/areas
                  const resultUrl = '@Url.Action("SalaryAnalysisResultSummary","SalaryAnalyzer")'
                                    + "?fileUploadid=" + encodeURIComponent(fileUploadId);

                  // Optional toast before redirect
                  appalert(response.message || "Analysis complete.", 1, 1);

                  // Redirect to the results page
                  window.location.assign(resultUrl); // or window.location.replace(resultUrl);
                } else {
                  appalert((response && response.message) || "Analysis failed.", 3, 1);
                }
              },
              error: function (err) {
                appalert(err.statusText || "Request failed.", 3, 1);
              }
            });
          },
          function () {
            appalert("Analysis cancelled", 3, 1);
          }
    );
  }

</script>
}





