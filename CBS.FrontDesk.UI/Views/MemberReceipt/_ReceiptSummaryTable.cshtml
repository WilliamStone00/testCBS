@model CBS.FrontDesk.Data.Entity.SavingProducts.AccountOperation.PaymentReceipt
    @using Resources
<style>
    .section-header {
        font-weight: bold;
        color: #4CAF50;
        margin-top: 1rem;
        margin-bottom: .5rem;
        display: flex;
        align-items: center;
        gap: 5px;
    }

    .table-sm th.bg-label {
        background-color: #EDEDED;
        width: 200px;
        font-weight: 600;
        text-transform: uppercase;
    }

    .table-sm td, .table-sm th {
        vertical-align: middle;
    }
</style>
<div class="px-3 py-2">

    <!-- 🔷 Receipt Title -->
    @if (!string.IsNullOrEmpty(Model.ReceiptTitle))
    {
        <h5 class="fw-bold text-uppercase text-primary border-bottom mb-3">
            <i class="mdi mdi-receipt-text me-1"></i> @Model.ReceiptTitle
        </h5>
    }

    <!-- 🔷 Member Information -->
<h6 class="fw-bold border-bottom text-primary">
    <i class="mdi mdi-account-circle-outline me-1"></i> @GlobalShared.Member_Information
</h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr><th class="bg-light" style="width: 220px;">@GlobalShared.Member_name</th><td>@Model.MemberName</td></tr>
            <tr><th class="bg-light">@GlobalShared.Member_reference</th><td>@Model.MemberReference</td></tr>
        </tbody>
    </table>

    <!-- 🔷 Depositor Information -->
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-account-box-outline me-1"></i> @GlobalMemberReceipt.depositor_information
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr><th class="bg-light" style="width: 220px;">@GlobalMemberReceipt.depositor_name</th><td>@Model.DepositorName</td></tr>
            <tr><th class="bg-light">@GlobalShared.Phone</th><td>@Model.DepositorPhone</td></tr>
            <tr><th class="bg-light">CNI</th><td>@Model.DepositorCNI</td></tr>
        </tbody>
    </table>

    <!-- 🔷 Account Information -->
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-bank-outline me-1"></i> @GlobalShared.Account_Information
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr>
                <th class="bg-light" style="width: 220px;">@GlobalMemberReceipt.total_account_balances</th>
                <td>@Model.TotalAccountBalances</td>
            </tr>
        </tbody>
    </table>

    <!-- 🔷 Cashier Information -->
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-human-greeting me-1"></i> @GlobalMemberReceipt.cashier_information
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr><th class="bg-light" style="width: 220px;">@GlobalShared.Cashier</th><td>@Model.CashierName</td></tr>
            <tr><th class="bg-light">@GlobalShared.till</th><td>@Model.TillName</td></tr>
        </tbody>
    </table>

    <!-- 🔷 Teller Information -->
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-account-badge-outline me-1"></i> @GlobalShared.teller_information
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr><th class="bg-light" style="width: 220px;">@GlobalShared.teller_id</th><td>@Model.TellerId</td></tr>
            <tr><th class="bg-light">@GlobalShared.Branch</th><td>@Model.BranchId</td></tr>
            <tr><th class="bg-light">@GlobalShared.Bank_ID</th><td>@Model.BankId</td></tr>
        </tbody>
    </table>

    <!-- 🔷 Transaction Details -->
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-cash-multiple me-1"></i> @GlobalShared.transaction_details
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <tbody>
            <tr><th class="bg-light" style="width: 220px;">@GlobalShared.transaction_date</th><td>@Model.Date.ToString("dd/MM/yyyy HH:mm")</td></tr>
            <tr><th class="bg-light">@GlobalShared.accounting_day</th><td>@Model.AccountingDay.ToString("dd/MM/yyyy")</td></tr>
            <tr><th class="bg-light">@GlobalShared.service_type</th><td>@Model.ServiceType</td></tr>
            <tr><th class="bg-light">@GlobalShared.Operation_type</th><td>@Model.OperationType</td></tr>
            <tr><th class="bg-light">@GlobalMemberReceipt.operation_grouping</th><td>@Model.OperationTypeGrouping</td></tr>
            <tr><th class="bg-light">@GlobalShared.receipt_type</th><td>@Model.ReceiptType</td></tr>
            <tr><th class="bg-light">@GlobalMemberReceipt.internal_ref</th><td>@Model.InternalReferenceNumber</td></tr>
            <tr><th class="bg-light">@GlobalMemberReceipt.external_ref</th><td>@Model.ExternalReferenceNumber</td></tr>
            <tr><th class="bg-light">@GlobalShared.source_of_request</th><td>@Model.SourceOfRequest</td></tr>
            <tr><th class="bg-light">@GlobalMemberReceipt.portal_used</th><td>@Model.PortalUsed</td></tr>
            <tr><th class="bg-light">@GlobalShared.Amount</th><td><strong>@Model.Amount.ToString("N0") FCFA</strong></td></tr>
            <tr><th class="bg-light">@GlobalShared.Charges</th><td>@Model.Charges.ToString("N0") FCFA</td></tr>
            <tr><th class="bg-light">@GlobalShared.Total_Paid</th><td class="fw-bold text-success">@Model.TotalAmount.ToString("N0") FCFA</td></tr>
            <tr><th class="bg-light">@GlobalMemberReceipt.in_words</th><td>@Model.AmountInWord</td></tr>
        </tbody>
    </table>
</div>

<!-- 🔷 Breakdown Table -->
@if (Model.PaymentDetails != null && Model.PaymentDetails.Any())
{
    <h6 class="fw-bold mt-4 border-bottom text-primary">
        <i class="mdi mdi-format-list-bulleted-type me-1"></i> @GlobalShared.Breakdown
    </h6>
    <table class="table table-bordered table-sm">
        <thead class="table-light">
            <tr>
                <th>@GlobalShared.Service</th>
                <th>@GlobalShared.Amount</th>
                @if (Model.ServiceType?.ToLower().Contains("loan") == true)
                {
                    <th>@GlobalShared.Interest</th>
                    <th>@GlobalShared.VAT</th>
                }
                <th>@GlobalShared.Fee</th>
                <th>@GlobalShared.Account_No.</th>
                <th>@GlobalShared.Balance</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.PaymentDetails)
            {
                <tr>
                    <td>@item.SericeName</td>
                    <td>@item.Amount.ToString("N0")</td>
                    @if (Model.ServiceType?.ToLower().Contains("loan") == true)
                    {
                        <td>@item.Interest.ToString("N0")</td>
                        <td>@item.VAT.ToString("N0")</td>
                    }
                    <td>@item.Fee.ToString("N0")</td>
                    <td>@item.AccountNumber</td>
                    <td>@item.AccountBalance</td>
                </tr>
            }
        </tbody>
        <tfoot class="table-light">
            <tr>
                <th>@GlobalShared.Total</th>
                <th>@Model.PaymentDetails.Sum(x => x.Amount).ToString("N0")</th>
                @if (Model.ServiceType?.ToLower().Contains("loan") == true)
                {
                    <th>@Model.PaymentDetails.Sum(x => x.Interest).ToString("N0")</th>
                    <th>@Model.PaymentDetails.Sum(x => x.VAT).ToString("N0")</th>
                }
                <th>@Model.PaymentDetails.Sum(x => x.Fee).ToString("N0")</th>
                <th colspan="2"></th>
            </tr>
        </tfoot>
    </table>
}

<!-- 🔷 Denomination Sections -->
@{
    var notes = new[] {
        ("10,000", Model.Note10000, Model.Note10000 * 10000),
        ("5,000", Model.Note5000, Model.Note5000 * 5000),
        ("2,000", Model.Note2000, Model.Note2000 * 2000),
        ("1,000", Model.Note1000, Model.Note1000 * 1000),
        ("500", Model.Note500, Model.Note500 * 500)
    }.Where(x => x.Item2 > 0).ToList();

    var coins = new[] {
        ("500 coin", Model.Coin500, Model.Coin500 * 500),
        ("350 coin", Model.Coin350, Model.Coin350 * 350),
        ("250 coin", Model.Coin250, Model.Coin250 * 250),
        ("200 coin", Model.Coin200, Model.Coin200 * 200),
        ("150 coin", Model.Coin150, Model.Coin150 * 150),
        ("100 coin", Model.Coin100, Model.Coin100 * 100),
        ("50 coin", Model.Coin50, Model.Coin50 * 50),
        ("25 coin", Model.Coin25, Model.Coin25 * 25),
        ("10 coin", Model.Coin10, Model.Coin10 * 10),
        ("5 coin", Model.Coin5, Model.Coin5 * 5),
        ("1 coin", Model.Coin1, Model.Coin1 * 1)
    }.Where(x => x.Item2 > 0).ToList();
}

@if (notes.Any())
{
    <h6 class="fw-bold mt-4 border-bottom text-primary">
        <i class="mdi mdi-cash me-1"></i> @GlobalMemberReceipt.denominations_notes
    </h6>
    <table class="table table-sm table-bordered mb-3">
        <thead class="table-light">
            <tr>
                <th>@GlobalShared.Denomination</th>
                <th>Qty</th>
                <th>@GlobalShared.Total</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var note in notes)
            {
                <tr>
                    <td>@note.Item1 FCFA</td>
                    <td>@note.Item2</td>
                    <td>@note.Item3.ToString("N0")</td>
                </tr>
            }
        </tbody>
        <tfoot class="table-light">
            <tr>
                <th colspan="2">@GlobalShared.total_notes</th>
                <th>@notes.Sum(x => x.Item3).ToString("N0")</th>
            </tr>
        </tfoot>
    </table>
}

@if (coins.Any())
{
    <h6 class="fw-bold border-bottom text-primary">
        <i class="mdi mdi-coin me-1"></i> @GlobalMemberReceipt.denominations_coins
    </h6>
    <table class="table table-sm table-bordered">
        <thead class="table-light">
            <tr>
                <th>@GlobalShared.Denomination</th>
                <th>Qty</th>
                <th>@GlobalShared.Total</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var coin in coins)
            {
                <tr>
                    <td>@coin.Item1</td>
                    <td>@coin.Item2</td>
                    <td>@coin.Item3.ToString("N0")</td>
                </tr>
            }
        </tbody>
        <tfoot class="table-light">
            <tr>
                <th colspan="2">@GlobalShared.total_coins</th>
                <th>@coins.Sum(x => x.Item3).ToString("N0")</th>
            </tr>
        </tfoot>
    </table>
}
