@model CBS.FrontDesk.Data.AccountBalanInitConfiguration

<div class="modal-body bg-light card-border-shadow-dark">
    @using (Html.BeginForm("Create", "AccountBalanceInitialization", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "return AjaxPosting(this);" }))
    {
        @Html.ValidationSummary(true, "Please correct the errors and try again.", new { @class = "alert alert-danger" })

        <div class="row gy-3">

            <!-- 🏢 Branch Selection -->
            <div class="col-12">
                <h6 class="text-primary border-bottom pt-2 pb-1">
                    <i class="mdi mdi-office-building-marker-outline me-1"></i> Branch & Account Type
                </h6>
            </div>

            <div class="col-md-6">
                <div class="form-floating shadow-sm">
                    @Html.DropDownListFor(m => m.InitInfoDto.BranchId, new SelectList(ViewBag.Branches, "Value", "Text"), "--- Select Branch ---", new { @class = "form-control select2" })
                    @Html.ValidationMessageFor(m => m.InitInfoDto.BranchId, "", new { @class = "text-danger" })
                    <label>Branch</label>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating shadow-sm">
                    @Html.DropDownListFor(m => m.InitInfoDto.ProductId, new SelectList(ViewBag.AccountTypes, "Value", "Text"), "--- Select Account Type ---", new { @class = "form-control select2" })
                    @Html.ValidationMessageFor(m => m.InitInfoDto.ProductId, "", new { @class = "text-danger" })
                    <label>Account Type</label>
                </div>
            </div>

            <!-- 💰 Balances -->
            <div class="col-12">
                <h6 class="text-primary border-bottom pt-2 pb-1">
                    <i class="mdi mdi-cash-multiple me-1"></i> Balances
                </h6>
            </div>

            <div class="col-md-6">
                <div class="form-floating shadow-sm">
                    @Html.TextBoxFor(m => m.InitInfoDto.SubmittedMemberBalance, new { @class = "form-control", @id = "SubmittedMemberBalance", placeholder = "Total Member Balance" })
                    @Html.ValidationMessageFor(m => m.InitInfoDto.SubmittedMemberBalance, "", new { @class = "text-danger" })
                    <label>Total Member Balance</label>
                </div>
            </div>

            <div class="col-md-6">
                <div class="form-floating shadow-sm">
                    @Html.TextBoxFor(m => m.InitInfoDto.SubmittedGLBalance, new { @class = "form-control", @id = "SubmittedGLBalance", placeholder = "GL Balance" })
                    @Html.ValidationMessageFor(m => m.InitInfoDto.SubmittedGLBalance, "", new { @class = "text-danger" })
                    <label>General Ledger Balance</label>
                </div>
            </div>

            <!-- 📑 Scenario -->
            <div class="col-12">
                <h6 class="text-primary border-bottom pt-2 pb-1">
                    <i class="mdi mdi-script-text-outline me-1"></i> Scenario
                </h6>
            </div>

            <div class="col-md-12">
                <div class="form-floating shadow-sm">
                    @Html.TextAreaFor(m => m.InitInfoDto.ScenarioId, new { @class = "form-control", @style = "height: 200px;" })
                    @Html.ValidationMessageFor(m => m.InitInfoDto.ScenarioId, "", new { @class = "text-danger" })
                    <label>Scenario</label>
                </div>
            </div>

            <!-- ✅ Submit -->
            <div class="col-12 text-end mt-3">
                <button type="submit" class="btn btn-primary shadow-sm px-4">
                    <i class="fas fa-check-circle me-1"></i> Submit Initialization
                </button>
                <button type="reset" class="btn btn-outline-secondary shadow-sm px-4">
                    Reset
                </button>
            </div>

        </div> <!-- .row -->
    }
</div>
