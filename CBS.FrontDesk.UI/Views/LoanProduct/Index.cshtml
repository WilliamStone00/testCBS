

@{
    ViewBag.Title = "Loan Product";
}
<div class="row">

    <div class="col-xl-12 col-lg-12 col-md-12 order-0 order-md-1">
        <div class="card">
            <div id="accordionPopoutIcon" class="accordion mt-3 accordion-popout">
                <div class="accordion-item active">
                    <h2 class="accordion-header text-body d-flex justify-content-between"
                        id="accordionPopoutIconThree">
                        <button type="button"
                                class="accordion-button"
                                data-bs-toggle="collapse"
                                data-bs-target="#accordionPopoutIcon-3"
                                aria-expanded="true"
                                aria-controls="accordionPopoutIcon-3">
                            <i class="mdi mdi-cash-multiple me-2"></i> Loan Product
                        </button>
                    </h2>
                    <div id="accordionPopoutIcon-3"
                         class="accordion-collapse collapse show"
                         data-bs-parent="#accordionPopoutIcon">
                        <div class="accordion-body">
                            <div id="mainviewChange">
                                <div class="change-group">
                                    <div class="card mb-4">
                                        <div class="card-body d-sm-flex d-block">
                                            <div class="d-flex align-items-center lh-1 me-3 mb-3 mb-sm-0 ms-auto">
                                                <div class="ms-3">
                                                    <a type="button" onclick="LoadDataGen('LoanProduct','myDataTable','_Data',0,'datalistingview',null,null,'list')" class="btn btn-icon btn-label-primary btn-sm btn-fab demo" data-bs-toggle="tooltip" title="View List">
                                                        <span class="tf-icons mdi mdi-view-list mdi-18px"></span>
                                                    </a>
                                                </div>
                                            </div>
                                        </div>
                                    </div>
                                    <div id="datalistingview">
                                        @Html.Partial("_LoanProduct")
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>



            </div>
        </div>
    </div>
</div>












@section Scripts {@Scripts.Render("~/bundles/jqueryval")
<script>
    function preserveSelectionOrder(selectElement) {
        // Get the selected values and their order
        var selectedValues = Array.from(selectElement.selectedOptions).map(option => option.value);

        // Remove all options from the select element
        while (selectElement.options.length > 0) {
            selectElement.remove(0);
        }

        // Add the options back in the original order
        var refundOrders = @Html.Raw(Json.Encode(ViewBag.RefundOrders));
        refundOrders.forEach(function (order) {
            var option = document.createElement("option");
            option.value = order.Value;
            option.text = order.Text;
            if (selectedValues.includes(order.Value)) {
                option.selected = true;
            }
            selectElement.add(option);
        });
    }
</script>
}


