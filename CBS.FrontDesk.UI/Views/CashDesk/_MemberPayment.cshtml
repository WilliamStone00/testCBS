@model CBS.FrontDesk.Data.Entity.SavingProducts.AccountActivation.CashDesk
    @using Resources
<div class="table-responsive" id="use_member_account" style="width: 100%;>
    <table class="table table-hover" id="myDataTableT">
        <thead class="table-light">
            <tr>
                <th>@GlobalShared.Amount</th>
                <th>@GlobalShared.Total</th>
                <th>@GlobalShared.Confirm</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var d in Model.BulkDeposits.Where(x => x.AccountType.ToLower() != "loan").OrderBy(t => t.AccountType))
            {
                <tr>
                    <td>
                        <input type="number" class="form-control amount-input shadow-input-boxes" value="@d.Amount">
                    </td>
                    <td>
                        <span class="total-span">@d.Total</span>
                    </td>
                    <td>
                        <input type="checkbox" class="form-check-input">
                    </td>
                </tr>
            }
        </tbody>
        <tfoot>
            <tr>
                <td colspan="1" style="text-align: right;"><strong>@GlobalShared.Total:</strong></td>
                <td style="display:none;"></td>
                <td id="tableTotal">0</td>
                <td></td>
            </tr>
            <tr>
                <td colspan="1" style="text-align: right;"><strong>@GlobalShared.Difference:</strong></td>
                <td id="tableBalance">0</td>
                <td></td>
            </tr>
        </tfoot>
    </table>
</div>

