@model CBS.FrontDesk.Data.Entity.AccountingDayObject.GetAccountingDayQuery


<fieldset class="fieldset-custom">
    <legend id="select_basex">Accounting Day Status Query Parameters</legend>
    <div class="card-body">
        @using (Html.BeginForm("DownloadAccountingDayStatus", "AccountingDay", FormMethod.Post, new { enctype = "multipart/form-data", onsubmit = "return AjaxPostAndUpdatePrintTillStatus(this);" }))
        {
            <hr />
            <div class="row g-4">
                <!-- Group for Query By (All or By Branch) -->
                <div class="col-md-12">
                    <fieldset>
                        <legend>Query By</legend>
                        <div class="form-check form-check-inline">
                            @Html.RadioButtonFor(m => m.QueryParameter, "All", new { @class = "form-check-input", @id = "allRadio", @onclick = "toggleBranchDiv(false);" })
                            <label class="form-check-label" for="allRadio">All</label>
                        </div>
                        <div class="form-check form-check-inline">
                            @Html.RadioButtonFor(m => m.QueryParameter, "ByBranch", new { @class = "form-check-input", @id = "byBranchRadio", @onclick = "toggleBranchDiv(true);" })
                            <label class="form-check-label" for="byBranchRadio">Branch</label>
                        </div>
                    </fieldset>
                </div>
            </div>
            <hr />
            <div class="row g-4">
                <div class="col-md-4" id="branchDiv">
                    <label for="branchInput">Branch</label>
                    @Html.DropDownListFor(m => m.BranchId, new SelectList(ViewBag.Branches, "id", "name"), "---Select Branch---", new { @class = "form-control shadow-box select2", @id = "branchInput" })
                    @Html.ValidationMessageFor(m => m.BranchId, "", new { @class = "text-danger" })
                </div>
                <div class="col-md-8">
                    <div class="row g-4">
                        <div class="col-md-6">
                            <label for="dateFromInput">Date From</label>
                            <div class="input-group">
                                @Html.TextBoxFor(m => m.DateFrom, new { @class = "form-control shadow-box date-picker", @id = "dateFromInput", @placeholder = "Date From" })
                            </div>
                            @Html.ValidationMessageFor(m => m.DateFrom, "", new { @class = "text-danger" })
                        </div>
                        <div class="col-md-6">
                            <label for="dateToInput">Date To</label>
                            <div class="input-group">
                                @Html.TextBoxFor(m => m.DateTo, new { @class = "form-control shadow-box date-picker", @id = "dateToInput", @placeholder = "Date To" })
                            </div>
                            @Html.ValidationMessageFor(m => m.DateTo, "", new { @class = "text-danger" })
                        </div>
                    </div>
                </div>
            </div>
            <hr />
            <div class="text-center">
                <button type="submit" class="btn btn-primary btn-sm">
                    <i class="fa fa-print me-2"></i> Print
                </button>
                <button type="button" class="btn btn-secondary btn-sm" onclick="viewInDataTable()">
                    <i class="fa fa-table me-2"></i> View in Datatable
                </button>
            </div>
        }
        <br />
        @Html.Partial("_StatusData")
    </div>
</fieldset>


