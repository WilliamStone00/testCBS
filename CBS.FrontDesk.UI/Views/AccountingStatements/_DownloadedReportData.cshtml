@model CBS.FrontDesk.Data.Entity.Accounting.AccountingEntryQuery
    @using Resources
<div class="container">
    <div class="card-body">
        <fieldset class="fieldset-custom">
            <legend>@AccountingStatements_DownloadedReportData.DownloadedHistory</legend>
            <table class="table table-hover table-bordered table-sm" id="myDataTable">

                <thead>
                    <tr>
                        <th>@AccountingStatements_DownloadedReportData.ReportType</th>
                        <th>@AccountingStatements_DownloadedReportData.BranchOffice</th>
                        <th>@AccountingStatements_DownloadedReportData.DateCreated</th>
                        <th>@AccountingStatements_DownloadedReportData.FileName</th>
                        <th>@AccountingStatements_DownloadedReportData.FileSize</th>
                        <th>@AccountingStatements_DownloadedReportData.Action</th>
                    </tr>
                </thead>
                <tbody>
                    <!-- Rows will be dynamically added here -->
                    @foreach (var d in Model.ReportDownloadInfo)
                    {
                        string file = d.FileName + d.Extension;
                        <tr>
                            <td style="width:17%">@d.ReportType</td>
                            <td style="width:20%">@d.BranchName</td>
                            <td style="width:13%">@d.CreatedDate</td>
                            <td style="width:30%">@file </td>
                            <td style="width:10%">@d.Size</td>
                            <td style="width:10%">
                                <a href="/AccountingStatements/DownloadById?fileId=@d.Id" onclick="" class="mr-2 btn btn-primary" data-toggle="tooltip" data-placement="top" title="@GlobalShared.Select @d.FileName @GlobalShared.ToDownload"><i class="mdi mdi-download"></i>  </a> |
                                <a href="#"
                                   class="mr-2 btn btn-danger"
                                   data-toggle="tooltip"
                                   data-placement="top"
                                   title="Delete @d.FileName"
                                   onclick="DeleteRecordPage('AccountingStatements', '@d.Id', '_DownloadedReportData')">
                                    <i class="mdi mdi-delete"></i>
                                </a>
                            </td>
                        </tr>
                    }


                </tbody>
            </table>
            @if (Model.ReportDownloadInfo.Count() > 0)
            {
                <table class="table table-hover table-bordered table-sm" id="m00">

                    <thead>
                        <tr>

                            <th colspan="6" id="DeleteBtn">
                                <a href="#"
                                   class="mr-2 btn btn-danger"
                                   data-toggle="tooltip"
                                   data-placement="top"
                                   title="@AccountingStatements_DownloadedReportData.DeleteDownloadHistory"
                                   onclick="DeleteRecordPage('AccountingStatements', 'XXXXXX', '_DownloadedReportData')">
                                    <i class="mdi mdi-delete"></i>@AccountingStatements_DownloadedReportData.DeleteDownloadHistory
                                </a>
                            </th>
                        </tr>
                    </thead>
                    <tbody>
                    </tbody>
                </table>
            }


        </fieldset>

    </div>
</div>
