@*@model List<CBS.FrontDesk.Data.Entity.AccountingV2.LiaisonMappingV2.LiaisonMapping>*@

@*<fieldset class="fieldset-custom">
    <legend>Liaison Mappings</legend>

    @if (!Model.Any())
    {
        <div class="text-center my-3">
            <p>No liaison mappings have been set up yet.</p>
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-hover table-bordered align-middle text-nowrap" style="table-layout: fixed;" id="myDataTable">
                <thead class="table-light">
                    <tr>
                        <th style="width: 14%;">Creation Date</th>
                        <th style="width: 39%;">Source Branch & Account</th>
                        <th style="width: 39%;">Counterparty Branch & Account</th>
                        <th style="width: 8%;" class="text-center">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.CreatedDate</td>

                            <td>
                                <div><strong>@item.BranchName</strong></div>
                                <small class="text-muted d-block">
                                    <i class="fas fa-university me-1"></i>@item.DueFromAssetAccountName
                                    <br />
                                    <span class="text-secondary">@item.DueFromAssetAccountNumber</span>
                                </small>
                            </td>

                            <td>
                                <div><strong>@item.CounterpartyBranchName</strong></div>
                                <small class="text-muted d-block">
                                    <i class="fas fa-university me-1"></i>@item.DueToLiabilityAccountName
                                    <br />
                                    <span class="text-secondary">@item.DueToLiabilityAccountNumber</span>
                                </small>
                            </td>

                            <td class="text-center">
                                <button class="btn btn-sm btn-outline-primary"
                                        onclick="AddORUpdateGen('@item.Id','datalistingview','_Create','update','LiaisonMappingConfig')">
                                    <i class="fas fa-edit me-1"></i> Edit
                                </button>-->-->
                                <button class="btn btn-sm btn-outline-info"
                                        onclick="showLiaisonMappingDetails('@item.Id')">
                                    <i class="fas fa-info-circle me-1"></i> Details
                                </button>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</fieldset>*@


@*<script>
    function showLiaisonMappingDetails(id) {
        $('#liaisonMappingDetailsContent').html('<p>Loading...</p>');

        $.ajax({
            url: '/LiaisonMappingConfig/GetLiaisonMappingPartialView',
            type: 'GET',
            data: { Key: id },
            success: function (html) {
                $('#liaisonMappingDetailsContent').html(html);
                $('#liaisonMappingDetailsModal').modal('show');
            },
            error: function () {
                $('#liaisonMappingDetailsContent').html('<p>Error loading details.</p>');
                $('#liaisonMappingDetailsModal').modal('show');
            }
        });
    }
</script>*@