@model CBS.FrontDesk.Data.Entity.LoanConf.LoanApplication
    @using Resources
@{
    ViewBag.Title = @GlobalLoanApplication.LoanApplicationQueryOverview;
}

<!-- Page Header -->
<div class="card bg-light mb-2 shadow-sm">
    <div class="card-body text-center">
        <h1 class="card-title text-primary">
            <i class="mdi mdi-finance me-2"></i> @GlobalLoanApplication.LOAN_APPLICATION_QUERY_LOGS
        </h1>
        <p class="card-text">
            @GlobalLoanApplication.MonitorAndAnalyzeLoanApplications.
        </p>
        <hr style="border: 2px solid rgba(128, 128, 128, 0.5);" />
    </div>
</div>

<!-- Search & Filters Section -->
<div class="card mb-3 shadow-sm">
    <div class="card-header bg-light text-dark fw-bold d-flex justify-content-between align-items-center">
        <span>
            <i class="mdi mdi-filter-outline me-2"></i>@GlobalLoanApplication.LoanApplicationSearchAndFilters
        </span>
        <button id="toggleFilterButton" class="btn btn-sm btn-light" data-bs-toggle="collapse" data-bs-target="#filterContent">
            <i class="mdi mdi-chevron-down"></i> @GlobalLoanApplication.ShowFilters
        </button>
    </div>
    <div class="card-body collapse" id="filterContent">
        <div class="row">
            <!-- Date Filters -->
            <div class="col-md-3">
                <label for="dateFrom" class="form-label fw-bold">@GlobalLoanApplication.DateFrom:</label>
                <input type="text" id="dateFrom" class="form-control shadow-sm" placeholder="dd/MM/yyyy" />
            </div>
            <div class="col-md-3">
                <label for="dateTo" class="form-label fw-bold">@GlobalLoanApplication.DateTo:</label>
                <input type="text" id="dateTo" class="form-control shadow-sm" placeholder="dd/MM/yyyy" />
            </div>

            <!-- Loan Category & Target -->
            <div class="col-md-3">
                <label for="loanCategory" class="form-label fw-bold">@GlobalLoanApplication.LoanCategory:</label>
                <select id="loanCategory" class="form-select shadow-sm">
                    <option value="all">@GlobalLoanApplication.All</option>
                    <option value="Main_loan">@GlobalLoanApplication.MainLoans</option>
                    <option value="SSF">@GlobalLoanApplication.SpecialSavingFacilities</option>
                </select>
            </div>
            <div class="col-md-3">
                <label for="approvalStatus" class="form-label fw-bold">@GlobalLoanApplication.ApprovalStatus:</label>
                <select id="approvalStatus" class="form-select shadow-sm">
                    <option value="all">@GlobalLoanApplication.All</option>
                    <option value="Approved">@GlobalLoanApplication.Approved</option>
                    <option value="Pending">@GlobalLoanApplication.Pending Approval</option>
                    <option value="Rejected">@GlobalLoanApplication.Rejected</option>
                </select>
            </div>
        </div>

        <div class="row mt-3">
            <div class="col-md-3">
                <label for="searchCriteria" class="form-label fw-bold">@GlobalLoanApplication.SearchCriteria:</label>
                <input type="text" id="searchCriteria" class="form-control shadow-sm" placeholder="@GlobalLoanApplication.EnterCustomerNameBranchOrReference..." />
            </div>
            <div class="col-md-3">
                <label for="loanTarget" class="form-label fw-bold">@GlobalLoanApplication.LoanTarget:</label>
                <select id="loanTarget" class="form-select shadow-sm">
                    <option value="all">@GlobalLoanApplication.All</option>
                    <option value="Employee">@GlobalLoanApplication.Employee</option>
                    <option value="Government">@GlobalLoanApplication.Government</option>
                    <option value="Groups">@GlobalLoanApplication.Groups</option>
                </select>
            </div>

            <!-- Branch Selection -->
            <div class="col-md-6">
                <label for="branchInput" class="form-label fw-bold">@GlobalLoanApplication.Branch:</label>
                @Html.DropDownListFor(
                    m => m.BranchId,
                    new SelectList(ViewBag.Branches, "Id", "Name"),
                    "---"+ @GlobalLoanApplication.SelectAll+"---",
                    new { @class = "form-control shadow-sm select2", @id = "branchInput" }
                )
            </div>
        </div>

        <!-- Search & Reset Buttons -->
        <div class="row mt-3">
            <div class="col-md-12 d-flex justify-content-end">
                <button id="searchButton" class="btn btn-primary me-2">
                    <i class="mdi mdi-magnify me-1"></i> @GlobalShared.Search
                </button>
                <button id="resetButton" class="btn btn-secondary me-2">
                    <i class="mdi mdi-refresh me-1"></i> @GlobalShared.ResetFilter
                </button>
                <button id="exportButton" class="btn btn-success" onclick="exportLoanData()">
                    <i class="mdi mdi-file-excel me-1"></i> @GlobalShared.Export
                </button>
            </div>
        </div>

    </div>
</div>



<!-- Loan Applications Table -->
<div class="card shadow-sm" id="loanApplicationDataCard" style="display: none;">
    <div class="card-header bg-light text-dark fw-bold">
        <i class="mdi mdi-table me-2"></i> @GlobalLoanApplication.LoanApplicationDataTable
    </div>
    <div class="card-body">
        <div id="loanApplicationDataTableContainer">
            @Html.Partial("_LoanApplicationData")
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
    <script src="~/Scripts/Js/Crud/LoanApplicationHistory/LoanApplicationQuery.js"></script>
}
