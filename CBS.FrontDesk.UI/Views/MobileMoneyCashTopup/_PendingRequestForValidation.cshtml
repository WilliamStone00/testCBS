@model List<CBS.FrontDesk.Data.Entity.SavingProducts.MobileMoneyCashTopup>
<style>
    table {
        table-layout: auto;
    }

        table th, table td {
            white-space: nowrap; /* Prevent text from wrapping */
        }
</style>
<fieldset class="fieldset-custom">
    <legend id="select_base">Data listings</legend>
    @if (Model.Count() == 0)
    {
        <div class="text-center my-3">
            <p>No Data Found</p>
        </div>
    }
    else
    {
        <div class="table-responsive mb-3">
            <table class="table table-hover" id="myDataTable">
                <thead class="table-light">
                    <tr>
                        <th>Date</th>
                        <th>Amount</th>
                        <th>Operator</th>
                        <th>Source</th>
                        <th>Branch</th>
                        <th>Status</th>
                        <th>Initiated by</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var d in Model)
                    {
                        <tr>
                            <td>@d.RequestDate</td>
                            <td>@d.Amount.ToString("#,##0.0")</td>
                            <td>
                                @if (d.OperatorType == "MobileMoneyMTN")
                                {
                                    <span class="badge bg-facebook">MTN</span>
                                }
                                else
                                {
                                    <span class="badge bg-instagram">Orange</span>
                                }
                            </td>
                            <td>@d.SourceType</td> <!-- Corrected source column -->
                            <td>@d.Branch.Name</td>
                            <td>
                                @if (d.RequestApprovalStatus == "Approved")
                                {
                                    <span class="badge bg-info">Approved</span>
                                }
                                else
                                {
                                    <span class="badge bg-danger">@d.RequestApprovalStatus</span>
                                }
                            </td>
                            <td>@d.RequestInitiatedBy</td>
                            <td>
                                <a href="/MobileMoneyCashTopup/RequestValidation?Id=@d.Id" class="mr-2" data-toggle="tooltip" data-placement="top" title="Validate @d.Amount to topup @d.OperatorType">Validate</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</fieldset>
