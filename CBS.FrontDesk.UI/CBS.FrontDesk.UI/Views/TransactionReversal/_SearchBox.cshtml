@model CBS.FrontDesk.Data.Entity.SavingProducts.CashReplenishmentSubTeller
@using Resources
<fieldset class="fieldset-custom">
    <legend>@GlobalTransactionReversal.RequestFilters</legend>
    <hr />
    <div class="row g-4">
        <div class="col-md-6">
            <label for="branchInput">@GlobalTransactionReversal.Branch</label>
            @Html.DropDownListFor(m => m.BranchId, new SelectList(ViewBag.Branches, "id", "name"), "---"+ @GlobalTransactionReversal.SelectBranch+"---", new { @class = "form-control select2", @id = "branchInput" })
        </div>
        <div class="col-md-6">
            <div class="row g-4">
                <div class="col-md-6">
                    <label for="dateFromInput">@GlobalTransactionReversal.DateFrom</label>
                    <div class="input-group">
                        <input type="date" id="dateFromInput" class="form-control shadow-box date-picker" placeholder="@GlobalTransactionReversal.DateFrom">
                    </div>
                </div>
                <div class="col-md-6">
                    <label for="dateToInput">@GlobalTransactionReversal.DateTo</label>
                    <div class="input-group">
                        <input type="date" id="dateToInput" class="form-control shadow-box date-picker" placeholder="@GlobalTransactionReversal.DateTo">
                        <button class="btn btn-primary btn-sm" onclick="SearchByDates('_CashReplenishmentSubTellerData','datalistingview','SubTellerCashRequisition')">@GlobalShared.Search</button>

                    </div>
                </div>
            </div>
        </div>
    </div>
</fieldset>

