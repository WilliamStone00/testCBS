@model List<CBS.FrontDesk.Data.Entity.VaultManagement.Vault>
@using Resources
<fieldset class="fieldset-custom">
    <legend id="select_base">@GlobalVault.VAULTS</legend>
    @if (!Model.Any())
    {
        <div class="text-center my-3">
            <p>@GlobalVault.NoVaultsHaveBeenConfiguredYet.</p>
        </div>
    }
    else
    {
        <div class="table-responsive mb-3">
            <table class="table table-hover table-bordered align-middle" id="myDataTable">
                <thead class="table-light">
                    <tr>
                        <th style="width: 27%;">@GlobalVault.VaultName</th>
                        <th style="width: 5%;">B.Code</th>
                        <th style="width: 20%;" class="text-end">@GlobalVault.MaximumCapacity</th>
                        <th style="width: 20%;" class="text-end">@GlobalVault.CurrentBalance</th>
                        <th style="width: 8%;">@GlobalShared.Status</th>
                        <th style="width: 20%;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var vault in Model)
                    {
                        <tr>
                            <td>@vault.Name</td>
                            <td>@vault.BranchCode</td>
                            <td class="text-end">@vault.MaximumCapacity.ToString("#,##0.0") FCFA</td>
                            <td class="text-end">@vault.CurrentBalance.ToString("#,##0.0") FCFA</td>
                            <td>
                                @if (vault.IsActive)
                                {
                                    <span class="badge rounded-pill bg-success fs-6">
                                        <i class="fas fa-check-circle"></i> @GlobalShared.Active
                                    </span>
                                }
                                else
                                {
                                    <span class="badge rounded-pill bg-danger fs-6">
                                        <i class="fas fa-times-circle"></i> @GlobalVault.Inactive
                                    </span>
                                }
                            </td>
                            <td>
                                <div class="d-flex justify-content-center align-items-center gap-2">
                                    <!-- Edit Button -->
                                    <button type="button"
                                            class="btn btn-sm btn-outline-primary d-flex align-items-center"
                                            onclick="AddORUpdateGen('@vault.Id', 'datalistingview', '_Create', 'update', 'Vault', null, null)"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="top"
                                            title="@GlobalShared.Edit @vault.Name">
                                        <i class="fas fa-edit me-1"></i> @GlobalShared.Edit
                                    </button>

                                    <!-- Details Button -->
                                    <button type="button"
                                            class="btn btn-sm btn-outline-info d-flex align-items-center"
                                            onclick="showVaultDetails('@vault.Id')"
                                            data-bs-toggle="tooltip"
                                            data-bs-placement="top"
                                            title="@GlobalShared.ViewDetails">
                                        <i class="fas fa-info-circle me-1"></i> @GlobalShared.Details
                                    </button>

                                    <!-- Init Button -->
                                    <a type="button" href="/Vault/Initilization"
                                       class="btn btn-sm btn-outline-warning d-flex align-items-center"
                                       data-bs-toggle="tooltip"
                                       data-bs-placement="top"
                                       title="@GlobalVault.InitializationOf @vault.Name">
                                        <i class="fas fa-cogs me-1"></i> @GlobalVault.Init
                                    </a>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
</fieldset>
